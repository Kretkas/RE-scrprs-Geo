# Legacy snapshot notes

This folder is a copy of selected files from the old parser project.

Old project:
`/Users/uladkucapalau/.openclaw/workspace/projects/1. Парсер квартир`

Policy:
- old project was not modified;
- `logs/app.log` was intentionally not copied;
- `logs/analysis.md` was copied as useful documentation;
- `seen_myhome_ids.txt` and `seen_ss_ids.txt` are preserved for future SQLite migration;
- `seen_korter_ids.txt` was not present in the old project;
- folder `Бля, точно работает/` was preserved because it appears to contain known-good parser variants.
